{
  "name": "imgcook-cli",
  "private": true,
  "scripts": {
    "init": "lerna bootstrap",
    "publish": "lerna publish",
    "publishSkipGit": "lerna publish --skip-git",
    "test": "lerna run test",
    "genSub": "git submodule init && git submodule update"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
